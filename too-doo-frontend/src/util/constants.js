const ERR_FIELD = 'nonFieldErrors';

const LINKS = {
  home: '/to_dos',
  signIn: '/sign_in',
  signUp: '/sign_up',

  newToDo: '/to_dos/new'
};

const NEW_ROUTE = 'new';

const OOPS = 'Some temporary issues, please try again later';

const ENVS = {
  dev: 'development',
  prod: 'production',
};

const API_ROOT = '/api';

export {
  ERR_FIELD,
  LINKS,
  OOPS,
  ENVS,
  API_ROOT,
  NEW_ROUTE
};